@model AntreDeuxVinsModel.Region

@{
    ViewData["Title"] = Localizer["Delete"]+"-"+ Localizer["Regions"];
}

<h2>@Localizer["Delete"]</h2>

<h3>@Localizer["DeleteMsg"]</h3>
<div>
    <h4>@Localizer["Regions"]</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nom)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nom)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Pays.Nom)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Pays.Nom)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" name="Parent" value="@ViewBag.Parent" />
        <input type="submit" value="@Localizer["Delete"]" class="btn btn-default" /> |
        @if (@ViewBag.Parent == null)
        {
            <a asp-action="Index">@Localizer["Back"]</a> 
        }
        else
        {
            <a asp-action="Details" asp-controller="Pays" asp-route-id="@ViewBag.Parent" asp-area="BackOffice">@Localizer["Back"]</a>
        }
    </form>
</div>
